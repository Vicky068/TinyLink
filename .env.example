# Example environment variables for TinyLink
# PostgreSQL connection string
DATABASE_URL=postgres://user:password@localhost:5432/tinylink

# Optional base URL used by the frontend when constructing short links (e.g. https://tinylink.example)
# If not set, the server's origin is used by the UI.
BASE_URL=

# Port the server listens on (default 3000)
PORT=3000
